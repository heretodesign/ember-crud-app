
<form onsubmit={{action "editPost"}}>
	<div>
		<label for="title">Title</label>
		{{input type="text" id="title" value=model.title}}
	</div>
	<div>
		<label for="post">Post</label>
		{{textarea rows="8" cols="80" value=model.body}}
	</div>

	<button type="submit">Publish</button>
</form>